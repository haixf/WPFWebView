<Window x:Class="WPFWebView.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFWebView"
        xmlns:web="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border Grid.Row="0"
                Background="#F0F0F0"
                Padding="8">
            <DockPanel>
                <Button x:Name="btnMenu"
         Width="40"
         Height="24"
         Padding="0"
         Background="Transparent"
         DockPanel.Dock="Right"
         Margin="0,0,8,0"
         Click="OnMenuClick"
         VerticalAlignment="Center" HorizontalAlignment="Right">
                    <TextBlock>Menu</TextBlock>
                    <Button.ContextMenu>
                        <ContextMenu>
                            <MenuItem Header="刷新" Click="OnRefresh"/>
                            <MenuItem Header="網址" Click="OnShowUrl"/>
                            <MenuItem Header="開發工具" Click="OnDeveloperTools"/>
                        </ContextMenu>
                    </Button.ContextMenu>
                </Button>
            </DockPanel>
        </Border>
        <web:WebView2 x:Name="webView" Source="https://icivetapps.foxconn.com/iapps/civetdeve/MobileJs.PDSS.htm"
                     Grid.Row="1"/>
    </Grid>
</Window>
